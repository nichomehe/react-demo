import App from './App';

const routes = [
    {
        path: '/',
        component: App,
        exact: true,

    },
    {
        path: '/index',
        component: App,
        exact: true,

    }
]

export default routes